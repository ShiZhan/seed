#!/usr/bin/env python
# -*- coding: utf-8 -*-

"""mknode -- SEED node initialization"""

from uuid import uuid1 as _genid
from uuid import getnode as _getnode

from model import Model, _seed_base, _node_model_file
from model import RDF, RDFS, OWL, XSD, SEED
from model import Literal, BNode
from utils import DEFAULT_ROOT
from utils import _SEED_LOG, _init_logger

def main():
    """mknode program"""

    _init_logger()

    _SEED_LOG.info('creating node model ...')

    model = Graph()
    model.load(_node_model_file)

    _SEED_LOG.info('loaded %d triples.' % len(model))


    _SEED_LOG.info('saving node ...')
    model.serialize(_node_model_file)


if __name__ == '__main__':
    main()
